# Sale Order System Documentation

ระบบใบสั่งขายสำหรับธุรกิจเครื่องประดับ - สร้างใบสั่งขายจากใบเสนอราคา

## โครงสร้างไฟล์

```
sale-order/
├── index-view.vue                    # หน้าหลัก - จัดการ state และ event
├── components/
│   ├── sale-order-view.vue          # ฟอร์มสร้างใบสั่งขายและตารางสินค้า
│   └── search-view.vue              # ฟอร์มค้นหาสินค้าและใบเสนอราคา
├── modal/
│   ├── quotation-search-modal.vue   # Modal เลือกใบเสนอราคา
│   ├── confirm-stock-modal.vue      # Modal ยืนยันสินค้าที่จะขาย
│   └── invoice-modal.vue            # Modal สร้าง Invoice จาก Sale Order
└── saleorder.md                     # เอกสารการใช้งาน
```

## ฟีเจอร์หลัก

### 1. การค้นหาและเลือกข้อมูล (Search & Selection)

#### การค้นหาใบเสนอราคา
- **เลขที่ใบเสนอราคา**: ค้นหาและโหลดข้อมูลจากใบเสนอราคา
- **ปุ่มใบเสนอราคา**: เปิด Modal เลือกจากรายการใบเสนอราคา

#### การค้นหาสินค้า
- **เลขที่ผลิต (ใหม่)**: รูปแบบ `DK-2502-00X`
- **เลขที่ผลิต (เก่า)**: รูปแบบ `AD054XX`
- **รหัสสินค้า**: รูปแบบ `R08X50XXXL`

### 2. ข้อมูลใบสั่งขาย (Sale Order Information)

#### ข้อมูลพื้นฐาน
- **เลขที่ใบสั่งขาย**: หมายเลขใบสั่งขาย (เช่น SO-2025-001)
- **วันที่ใบสั่งขาย**: วันที่สร้างใบสั่งขาย
- **วันที่คาดหวังส่งมอบ**: วันที่ที่ลูกค้าต้องการรับสินค้า
- **สถานะ**: สถานะปัจจุบันของใบสั่งขาย

#### ข้อมูลการชำระเงิน
- **อ้างอิงใบเสนอราคา**: เลขที่ใบเสนอราคาที่อ้างอิง (read-only)
- **เงื่อนไขการชำระเงิน**: Net 15/30/60 วัน, เงินสด, เครดิต
- **เงินมัดจำ (%)**: เปอร์เซ็นต์เงินมัดจำที่ต้องชำระล่วงหน้า
- **ลำดับความสำคัญ**: ปกติ, สำคัญ, เร่งด่วน, วิกฤต

#### ตัวเลือกสถานะ
| สถานะ | ความหมาย |
|-------|-----------|
| ร่าง | ยังไม่เสร็จสมบูรณ์ |
| รอการยืนยัน | รอลูกค้าหรือผู้บริหารยืนยัน |
| ยืนยันแล้ว | ยืนยันใบสั่งขายแล้ว |
| กำลังผลิต | อยู่ระหว่างการผลิต |
| พร้อมส่งมอบ | ผลิตเสร็จแล้ว พร้อมส่งมอบ |
| ส่งมอบแล้ว | ส่งมอบสินค้าเรียบร้อยแล้ว |
| ยกเลิก | ยกเลิกใบสั่งขาย |

### 3. ข้อมูลลูกค้า (Customer Information)

#### การแสดงข้อมูล (Read-only)
- **ชื่อลูกค้า**: ชื่อลูกค้าจากใบเสนอราคา
- **ที่อยู่**: ที่อยู่ติดต่อ
- **เบอร์โทร**: หมายเลขโทรศัพท์
- **อีเมล**: ที่อยู่อีเมล

*หมายเหตุ: ข้อมูลลูกค้าจะถูกโหลดจากใบเสนอราคาที่เลือก*

### 4. ตารางรายการสินค้า (Items Table)

#### คอลัมน์หลัก
| คอลัมน์ | รายละเอียด | แก้ไขได้ |
|---------|------------|----------|
| รูปภาพ | แสดงรูปสินค้า 25x25px | ❌ |
| รหัสสินค้า | รหัสสินค้า/ProductNumber | ❌ |
| รายละเอียด | คำอธิบายสินค้า | ❌ |
| วัตถุดิบ | ทอง, เพชร, พลอย (น้ำหนัก+จำนวน) | ❌ |
| ราคาต่อหน่วย | ราคาต่อชิ้น | ❌ |
| จำนวน | จำนวนสินค้าที่สั่ง | ✅ |
| ราคารวม | ราคารวมต่อรายการ | ❌ |
| สถานะการขาย | สถานะการยืนยันการขาย | ❌ |

#### สถานะการขาย
- **รอยืนยัน**: สินค้ายังไม่ได้ยืนยันการขาย (badge สีเหลือง)
- **ยืนยันแล้ว**: สินค้าได้รับการยืนยันการขายแล้ว (badge สีเขียว)

#### การดำเนินการ
- **ลบ**: ลบรายการออกจากตาราง
- **แก้ไข**: แก้ไขข้อมูลสินค้า
- **คัดลอก**: คัดลอกรายการเป็น Copy Item

### 5. การคำนวณราคา (Price Calculation)

#### สูตรการคำนวณ
```javascript
// ราคารวมต่อรายการ
itemTotal = unitPrice × quantity

// ยอดรวมก่อนส่วนลด
subtotal = Σ(itemTotal)

// ส่วนลด
discountAmount = subtotal × (discountPercent / 100)

// ยอดรวมสุทธิ
grandTotal = subtotal - discountAmount + freight

// เงินมัดจำ
advancePaymentAmount = grandTotal × (advancePaymentPercent / 100)

// ยอดค้างชำระ
remainingAmount = grandTotal - advancePaymentAmount
```

#### สรุปวัตถุดิบ
- **ทองรวม**: น้ำหนักทองรวมทั้งหมด (gms)
- **เพชรรวม**: น้ำหนักเพชรรวม (cts)
- **พลอยรวม**: น้ำหนักพลอยรวม (cts)

#### สรุปราคา
- **ยอดรวมก่อนส่วนลด**: ราคารวมก่อนหักส่วนลด
- **ส่วนลด**: จำนวนเงินส่วนลด
- **ค่าขนส่ง**: ค่าขนส่งและประกัน
- **ยอดรวมสุทธิ**: ราคาสุทธิที่ลูกค้าต้องจ่าย
- **เงินมัดจำ**: จำนวนเงินมัดจำ
- **ยอดค้างชำระ**: จำนวนเงินที่ค้างชำระ

### 6. Modal เลือกใบเสนอราคา (Quotation Search Modal)

#### โครงสร้าง Modal
- **Modal Component**: ใช้ `@/components/modal/ModalView.vue` 
- **Pattern**: ตามแบบ standard modal ในระบบ (เช่น update-plan-view.vue)
- **Width**: 90% ของหน้าจอ, fitHeight: true
- **Title**: "เลือกใบเสนอราคา" ใน header

#### ฟีเจอร์การค้นหา
- **การค้นหาหลัก**: 
  - เลขที่ใบเสนอราคา (แสดงในหน้าหลัก)
- **การค้นหาขั้นสูง**: (Dialog inside Modal)
  - ชื่อลูกค้า
  - สกุลเงิน (USD, THB)
  - ผู้สร้าง
  - วันที่สร้าง (ช่วงวันที่)
  - วันที่ใบเสนอราคา (ช่วงวันที่)

#### ปุ่มการทำงาน
- **🔍 ค้นหา**: ทำการค้นหาข้อมูลตามเงื่อนไข
- **🔍+ เพิ่มเติม**: เปิด dialog สำหรับตัวกรองเพิ่มเติม
- **❌ ล้าง**: ล้างเงื่อนไขการค้นหาทั้งหมด

#### คอลัมน์ในตาราง (BaseDataTable)
| คอลัมน์ | รายละเอียด | การจัดเรียง |
|---------|------------|-------------|
| การดำเนินการ | ปุ่ม "เลือก" สีเขียว | ❌ |
| เลขที่ใบเสนอราคา | หมายเลขใบเสนอราคา | ✅ |
| เลขที่รัน | เลขที่รันของระบบ | ✅ |
| ชื่อลูกค้า | ชื่อลูกค้า + เบอร์โทร | ✅ |
| สกุลเงิน | สกุลเงินที่ใช้ | ✅ |
| อัตราแลกเปลี่ยน | อัตราแลกเปลี่ยนเงิน | ✅ |
| Markup (%) | อัตรากำไร | ✅ |
| ส่วนลด (%) | เปอร์เซ็นต์ส่วนลด | ✅ |
| ค่าขนส่ง | ค่าขนส่งและประกัน | ✅ |
| วันที่ใบเสนอราคา | วันที่ของใบเสนอราคา | ✅ |
| วันที่สร้าง | วันที่สร้างในระบบ | ✅ |
| ผู้สร้าง | ชื่อผู้สร้าง | ✅ |

#### ฟีเจอร์ตาราง
- **Pagination**: แสดง 10 รายการต่อหน้าเป็นค่าเริ่มต้น
- **Sorting**: รองรับการจัดเรียงทุกคอลัมน์
- **Responsive**: ปรับขนาดตามหน้าจอ
- **ScrollHeight**: `calc(100vh - 450px)` สำหรับ Modal

#### การดำเนินการ
- **เลือก**: เลือกใบเสนอราคาและโหลดข้อมูลมาสร้าง Sale Order
- **ปิด Modal**: ปิด Modal โดยไม่เลือกข้อมูล

#### Technical Implementation
```vue
// Modal Structure
<modal :showModal="isShowModal" @closeModal="closeModal" :width="'90%'" :fitHeight="true">
  <template v-slot:content>
    <!-- Search Form -->
    <!-- Results Table -->
  </template>
</modal>

// API Integration
usrQuotationApiStore.fetchList({
  take: 10,
  skip: 0,
  sort: [],
  formValue: searchForm
})
```

### 7. ฟิลด์เพิ่มเติม (Additional Fields)

#### การตั้งค่าราคา
- **ส่วนลด (%)**: ส่วนลดเพิ่มเติมจากใบเสนอราคา (0-100%)
- **ค่าขนส่ง**: ค่าขนส่งและประกัน

#### ข้อมูลเพิ่มเติม
- **หมายเหตุ**: หมายเหตุสำหรับใบสั่งขาย

### 8. การดำเนินการ (Actions)

#### ปุ่มหลัก
- **ยืนยันการขาย**: ยืนยันสินค้าที่จะขาย - สีเหลือง
- **Invoice**: สร้าง Invoice PDF จากสินค้าที่ยืนยันแล้ว - สีเขียว
- **บันทึกร่าง**: บันทึกข้อมูลใบสั่งขายในสถานะร่าง - สีขาว
- **กลับรายการ**: กลับไปหน้ารายการใบสั่งขาย (ในโหมดดู) - สีเทา

## การใช้งาน

## 🔄 **Workflow ใหม่ (Updated Process)**

### **ลำดับการทำงาน:**
1. **สร้าง Sale Order** → 2. **ยืนยันการขาย Stock** → 3. **ออก Invoice**

```
┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐
│   สร้าง Sale Order   │ → │  ยืนยันการขาย Stock  │ → │     ออก Invoice      │
│   (Create SO)       │    │ (Confirm Stock)     │    │  (Generate Invoice) │
└─────────────────────┘    └─────────────────────┘    └─────────────────────┘
```

### **ปุ่มที่ใช้งาน:**
- 🟡 **ยืนยันการขาย** - เลือกสินค้าที่จะขาย
- 🟢 **Invoice** - สร้าง PDF จากสินค้าที่ยืนยันแล้ว
- ⚪ **บันทึกร่าง** - บันทึกข้อมูล Sale Order

### **หมายเหตุ:**
- ❌ **ลบแล้ว**: ปุ่ม "ยืนยันใบสั่งขาย" (ไม่ใช้แล้ว)
- ✅ **เปลี่ยน**: ไม่ต้องยืนยันใบสั่งขายก่อนยืนยันสินค้า
- ✅ **เปลี่ยน**: Invoice ทำได้เมื่อมีสินค้าที่ยืนยันแล้ว

---

### 1. สร้างใบสั่งขายจากใบเสนอราคา

#### วิธีที่ 1: ค้นหาด้วยเลขที่
1. กรอกเลขที่ใบเสนอราคาในช่องค้นหา
2. คลิกปุ่ม "ใบเสนอราคา" (สีเขียว)
3. ระบบจะโหลดข้อมูลจากใบเสนอราคาอัตโนมัติ

#### วิธีที่ 2: เลือกจาก Modal
1. คลิกปุ่ม "ใบเสนอราคา" โดยไม่กรอกเลขที่
2. Modal รายการใบเสนอราคาจะเปิดขึ้น
3. ค้นหาและเลือกใบเสนอราคาที่ต้องการ
4. คลิกปุ่ม "เลือก" ในรายการที่ต้องการ

### 2. การตั้งค่าข้อมูลใบสั่งขาย
1. **ข้อมูลพื้นฐาน**:
   - กรอกเลขที่ใบสั่งขาย
   - เลือกวันที่ใบสั่งขาย
   - กำหนดวันที่คาดหวังส่งมอบ
   - เลือกสถานะเริ่มต้น

2. **ข้อมูลการชำระเงิน**:
   - เลือกเงื่อนไขการชำระเงิน
   - กำหนดเปอร์เซ็นต์เงินมัดจำ
   - เลือกลำดับความสำคัญ

### 3. การปรับแต่งรายการสินค้า
1. **ปรับจำนวน**: แก้ไขจำนวนสินค้าในแต่ละรายการ
2. **ลบรายการ**: คลิกปุ่มลบเพื่อนำรายการออก
3. **เพิ่มสินค้า**: ใช้การค้นหาสินค้าเพิ่มรายการใหม่

### 4. การตั้งค่าราคาเพิ่มเติม
1. กำหนดส่วนลดเพิ่มเติม (ถ้ามี)
2. ระบุค่าขนส่งและประกัน
3. เพิ่มหมายเหตุประกอบ

### 5. การบันทึกใบสั่งขาย
1. **ตรวจสอบข้อมูล**: ยืนยันความถูกต้องของข้อมูลทั้งหมด
2. **บันทึกร่าง**: คลิกปุ่ม "บันทึกร่าง" เพื่อบันทึกข้อมูล

### 6. การยืนยันสินค้าที่จะขาย
1. **คลิกปุ่ม ยืนยันการขาย**: ปุ่มสีเหลืองในส่วนการดำเนินการ
2. **เลือกสินค้าที่จะยืนยัน**: 
   - Modal จะแสดงรายการสินค้าจาก Stock Items เท่านั้น (ไม่รวม Copy Items)
   - เลือกสินค้าที่พร้อมขายและมีในสต็อก
   - สามารถใช้ "เลือกทั้งหมด" หรือเลือกรายการแต่ละรายการ
   - สินค้าที่ยืนยันแล้วจะไม่สามารถเลือกซ้ำได้
3. **บันทึกการยืนยัน**: คลิกปุ่ม "ยืนยันสินค้าที่เลือก" เพื่อบันทึกข้อมูล
4. **ตรวจสอบสถานะ**: สินค้าที่ยืนยันแล้วจะแสดงสถานะ "ยืนยันแล้ว" ในตาราง

#### Confirm Stock Modal Features
- **การแสดงสถานะ**: แสดงสถานะ "ยืนยันแล้ว" หรือ "รอยืนยัน" ของแต่ละรายการ
- **การเลือกสินค้า**: เลือกได้เฉพาะสินค้าที่ยังไม่ได้ยืนยัน
- **การแสดงรูปภาพ**: แสดงรูปสินค้าเพื่อช่วยในการตัดสินใจ
- **การคำนวณยอดรวม**: แสดงยอดรวมของสินค้าที่เลือก
- **การเชื่อมต่อ API**: บันทึกข้อมูลการยืนยันผ่าน API

### 7. การสร้าง Invoice
1. **เงื่อนไข**: สามารถสร้าง Invoice ได้เฉพาะเมื่อมีสินค้าที่ยืนยันแล้ว
2. **คลิกปุ่ม Invoice**: ปุ่มสีเขียวในส่วนการดำเนินการ (จะ disabled หากไม่มีสินค้าที่ยืนยันแล้ว)
3. **เลือกสินค้า**: 
   - Modal จะแสดงเฉพาะสินค้าที่ยืนยันแล้ว (Stock Items ที่มีสถานะ "ยืนยันแล้ว")
   - เลือกสินค้าที่ต้องการออก Invoice
   - สามารถใช้ "เลือกทั้งหมด" หรือเลือกรายการแต่ละรายการ
4. **ตั้งค่าสกุลเงิน**: 
   - ปรับสกุลเงินและอัตราแลกเปลี่ยน (ถ้าจำเป็น)
   - ระบบจะแสดงราคาตามสกุลเงินที่เลือก
5. **สร้าง PDF**: คลิกปุ่ม "สร้าง Invoice PDF" เพื่อดาวน์โหลดไฟล์

#### Invoice Modal Features (Updated)
- **การกรองสินค้า**: แสดงเฉพาะสินค้าที่ยืนยันแล้ว
- **การเลือกสินค้า**: ตารางรายการสินค้าพร้อม checkbox
- **การคำนวณอัตโนมัติ**: คำนวณยอดรวมตามสินค้าที่เลือก
- **การแปลงสกุลเงิน**: แปลงราคาตามอัตราแลกเปลี่ยน
- **การตรวจสอบข้อมูล**: ตรวจสอบความถูกต้องก่อนสร้าง PDF
- **การสร้าง PDF**: ใช้ระบบ PDF ที่มีอยู่แล้ว (invoice-pdf-integration.js)

## ข้อมูลเทคนิค

### Dependencies
- **Vue 3**: Composition API และ Reactivity
- **PrimeVue**: DataTable, Calendar, Dropdown, Dialog components
- **Vue Router**: การนำทางระหว่างหน้า

### Styling Framework
- **SCSS**: ใช้ styling กลางจาก `@/assets/scss/`
- **Bootstrap Icons**: สำหรับไอคอนต่างๆ
- **Custom Components**: ใช้ component pattern เดียวกันกับระบบอื่น

### Component Architecture
```
SaleOrderIndexView (Main)
├── SearchView (Top search bar)
└── SaleOrderView (Main form)
    ├── Sale Order Information Card
    ├── Customer Information Card
    ├── Items DataTable Card
    ├── Summary Card
    └── Action Buttons
```

### State Management
- **Local State**: ใช้ Vue 3 data() และ computed properties
- **Props/Emit**: สำหรับ communication ระหว่าง components
- **Watchers**: ติดตาม changes และ update calculations

## การพัฒนาต่อ (Future Enhancements)

### API Integration
```javascript
// TODO: เชื่อมต่อ APIs
- fetchQuotationList()     // ดึงรายการใบเสนอราคา
- loadQuotationData()      // โหลดข้อมูลจากใบเสนอราคา
- saveSaleOrder()          // บันทึกใบสั่งขาย
- generatePDF()            // สร้าง PDF
- searchProducts()         // ค้นหาสินค้า
```

### Store Integration (Pinia)
```javascript
// ตัวอย่าง Store structure
const useSaleOrderStore = defineStore('saleOrder', {
  state: () => ({
    currentOrder: {},
    quotationList: [],
    productList: []
  }),
  actions: {
    async createFromQuotation(quotationId),
    async saveDraft(),
    async submitOrder(),
    async generatePDF()
  }
})
```

### Validation Rules
- **เลขที่ใบสั่งขาย**: ต้องไม่ซ้ำในระบบ
- **วันที่ส่งมอบ**: ต้องไม่เป็นอดีต
- **จำนวนสินค้า**: ต้องมากกว่า 0
- **เงินมัดจำ**: 0-100%

### Permission System
- **SALE_CREATE**: สร้างใบสั่งขายใหม่
- **SALE_EDIT**: แก้ไขใบสั่งขาย
- **SALE_DELETE**: ลบใบสั่งขาย
- **SALE_VIEW**: ดูใบสั่งขาย

## เส้นทางการนำทาง (Routing)

### URL
```
/sale-order
```

### Route Configuration
```javascript
{
  path: '/sale-order',
  name: 'sale-order',
  component: SaleOrder,
  meta: {
    Displayname: {
      en: 'Sale Order',
      th: 'ใบสั่งขาย'
    },
    minorShow: true,
    permissions: [PERMISSIONS.SALE_CREATE]
  }
}
```

## การบำรุงรักษา

### การเพิ่มฟิลด์ใหม่
1. เพิ่มใน `formSaleOrder` data structure
2. เพิ่ม input field ใน template
3. อัปเดต validation และ calculation logic

### การเพิ่มสถานะใหม่
1. เพิ่มใน `statusOptions` array
2. อัปเดต styling classes ตามต้องการ
3. เพิ่ม business logic สำหรับสถานะใหม่

### การปรับแต่ง UI
1. แก้ไข SCSS ใน style section
2. ใช้ standard classes จาก central SCSS files
3. ปรับ layout ใน card structures

---
*อัปเดตเมื่อ: 2025-10-03*
*เวอร์ชัน: 1.7.0*
*สถานะ: Stock Confirmation & Currency Rate Fix Complete*

## การอัปเดตล่าสุด (v1.6.0)

### ✨ Sale Order List & Invoice Generation v1.6.0
- **Sale Order List UI**: หน้ารายการใบสั่งขายพร้อมการค้นหาและกรองข้อมูล
- **Invoice Modal**: Modal เลือกสินค้าจาก stock items สำหรับสร้าง Invoice
- **PDF Generation**: ระบบสร้าง Invoice PDF อัตโนมัติ
- **View Mode**: โหมดดูรายละเอียดใบสั่งขาย (read-only)

### 🔧 Sale Order List Features v1.6.0
- **Search & Filter**: ค้นหาตามเลขที่ SO, ชื่อลูกค้า, สกุลเงิน, วันที่
- **Data Table**: แสดงข้อมูลแบบ pagination พร้อม sorting
- **Action Buttons**: แก้ไข, ดูรายละเอียด, สร้าง Invoice
- **Status Display**: แสดงสถานะด้วย color-coded badges

### 📄 Invoice PDF System v1.6.0
- **Stock Items Selection**: เลือกสินค้าจาก stock items เท่านั้น (ไม่รวม copy items)
- **Currency Support**: รองรับสกุลเงินต่างๆ พร้อมอัตราแลกเปลี่ยน
- **Professional Layout**: รูปแบบ Invoice มาตรฐานธุรกิจ
- **Auto Calculation**: คำนวณยอดรวมอัตโนมัติ

### 🎯 File Structure v1.6.0
```
saleorder-list/
├── index-view.vue                    # หน้าหลักรายการ SO
├── components/
│   ├── search-view.vue              # ฟอร์มค้นหาและกรอง
│   └── data-table-view.vue          # ตารางแสดงข้อมูล + ปุ่ม Invoice
├── modal/
│   └── invoice-modal.vue            # Modal เลือกสินค้าสำหรับ Invoice
└── saleorder-list.md               # เอกสารการใช้งาน

services/helper/pdf/invoice/
├── invoice-pdf-builder.js           # PDF generation logic
└── invoice-pdf-integration.js       # PDF service integration
```

### 📋 Invoice Modal Features v1.6.0
| ฟีเจอร์ | รายละเอียด |
|---------|-----------|
| Stock Items Only | เลือกเฉพาะสินค้าที่มี stockNumber |
| Select All/None | เลือกทั้งหมดหรือยกเลิกการเลือก |
| Real-time Total | คำนวณยอดรวมตามรายการที่เลือก |
| Currency Control | กำหนดสกุลเงินและอัตราแลกเปลี่ยน |
| PDF Download | ดาวน์โหลด Invoice PDF อัตโนมัติ |

### 🔒 View Mode Implementation v1.6.0
- **Read-only Fields**: ทุกฟิลด์เป็น readonly เมื่ออยู่ในโหมดดู
- **Hidden Buttons**: ซ่อนปุ่มแก้ไข (บันทึก, ยืนยัน, ล้าง, ยกเลิก)
- **Back Navigation**: ปุ่ม "กลับรายการ" สำหรับโหมดดู
- **Print Access**: ปุ่มพิมพ์ยังคงใช้งานได้

### 🎨 UI/UX Enhancements v1.6.0
```vue
<!-- Invoice Button in Action Menu -->
<button 
  class="btn btn-sm btn-info ml-2" 
  title="สร้าง Invoice" 
  @click="onCreateInvoice(data)"
>
  <i class="bi bi-receipt"></i>
</button>
```

### 📊 Technical Implementation v1.6.0
- **Modal Pattern**: ตาม quotation-search-modal structure
- **PDF Service**: ใช้ pdfMake สำหรับสร้าง PDF
- **Store Integration**: ใช้ sale-order-store สำหรับ API calls
- **Error Handling**: ระบบจัดการ error ครบถ้วน

### 🔄 Data Flow v1.6.0
```
Sale Order List → Click Invoice → Modal Open → Select Items → Generate PDF → Download
      ↓              ↓              ↓              ↓              ↓
1. Load SO data  2. Show modal  3. Load items  4. Validate    5. Create & download
```

## การอัปเดตก่อนหน้า (v1.5.0)

### ✨ SO Running Number Generation v1.5.0
- **Auto Generate SO Number**: ระบบสร้างเลขที่ใบสั่งขายอัตโนมัติผ่าน API
- **Readonly Input Field**: ป้องกันการ input free text เข้าช่องเลขที่ SO
- **Generate Button**: ปุ่ม "สร้าง" สำหรับสร้างเลขที่ SO ใหม่
- **Number Locking**: ล็อคเลขที่ SO หลังบันทึกร่างไม่ให้แก้ไขได้

### 🔧 API Implementation v1.5.0
- **Backend API**: `POST /SaleOrder/GenerateRunningNumber`
- **Service Method**: `GenerateRunningNumber()` ใน SaleOrderService
- **Running Number Service**: ใช้ prefix "SO" สำหรับเลขที่ใบสั่งขาย
- **Frontend Store**: `fetchGenerateRunningNumber()` ใน sale-order-store

### 🎯 UI/UX Changes v1.5.0
- **Input Field**: เปลี่ยนจาก text input เป็น readonly input
- **Generate Button**: ปุ่มสีฟ้า (btn-main) พร้อม icon plus-circle
- **Placeholder Text**: "กดปุ่มสร้างเลขที่ใบสั่งขาย"
- **Button States**: disable เมื่อ loading หรือ SO number ถูกล็อค

### 🔒 Validation & Security v1.5.0
- **Required SO Number**: ต้องสร้างเลขที่ SO ก่อนบันทึก
- **Lock After Save**: ไม่สามารถแก้ไขเลขที่ SO หลังบันทึกร่าง
- **Lock on Load**: เลขที่ SO ถูกล็อคเมื่อโหลดข้อมูลที่มีอยู่
- **Error Handling**: แสดงข้อความเมื่อไม่สามารถสร้างเลขที่ได้

### 📋 Technical Details v1.5.0
```javascript
// Frontend การสร้างเลขที่ SO
async generateSONumber() {
  const res = await this.saleOrderStore.fetchGenerateRunningNumber()
  this.formSaleOrder.number = res.data
  // Show success message
}

// Backend การสร้างเลขที่
public async Task<string> GenerateRunningNumber() {
  return await _runningNumberService.GenerateRunningNumberForGold("SO")
}
```

### 🎨 UI Components v1.5.0
```vue
<!-- SO Number Input Field -->
<div class="d-flex align-items-center">
  <input
    :class="['form-control bg-input mr-2']"
    type="text"
    v-model.trim="formSaleOrder.number"
    readonly
    :disabled="isSONumberLocked"
  />
  <button
    class="btn btn-main btn-sm"
    @click="generateSONumber"
    :disabled="loading || isSONumberLocked"
  >
    <i class="bi bi-plus-circle mr-1"></i>
    สร้าง
  </button>
</div>
```

### 🔄 Data Flow v1.5.0
```
1. User clicks "สร้าง" button
2. Call API: POST /SaleOrder/GenerateRunningNumber
3. Backend generates: SO2025001, SO2025002, etc.
4. Frontend updates: formSaleOrder.number
5. Save Draft: isSONumberLocked = true
6. Cannot modify SO number anymore
```

## การอัปเดตก่อนหน้า (v1.4.0)

### ✨ API Integration Complete v1.4.0
- **Sale Order Store**: เพิ่ม Pinia store สำหรับจัดการ API calls
- **Save/Update Functionality**: ระบบบันทึก/อัปเดตใบสั่งขายผ่าน API
- **Load Sale Order**: ฟังก์ชันโหลดใบสั่งขายที่บันทึกไว้แล้ว
- **API Error Handling**: จัดการ error และแสดงข้อความที่เหมาะสม

### 🔧 Technical Implementation v1.4.0
- **Store Integration**: ใช้ `usrSaleOrderApiStore` ตามแบบแผน quotation
- **API Mapping**: แปลงข้อมูลฟอร์มให้ตรงกับ API specification
- **JSON Data Storage**: เก็บรายการสินค้าในรูปแบบ JSON string
- **Status Management**: จัดการสถานะใบสั่งขาย (ร่าง, ยืนยันแล้ว)

### 📋 New API Methods v1.4.0
| Method | Description | Usage |
|--------|-------------|-------|
| `fetchSaveSaleOrder()` | บันทึก/อัปเดตใบสั่งขาย | รองรับทั้ง Draft และ Confirmed |
| `fetchGetSaleOrder()` | โหลดใบสั่งขายที่มีอยู่ | ใช้ soNumber ในการค้นหา |
| `loadSaleOrderData()` | แปลงข้อมูล API มาแสดงในฟอร์ม | Parse JSON และกำหนดค่าฟิลด์ต่างๆ |
| `saveDraft()` | บันทึกร่าง | เรียก fetchSaveSaleOrder('Draft') |
| `confirmOrder()` | ยืนยันใบสั่งขาย | เรียก fetchSaveSaleOrder('ยืนยันแล้ว') |

### 🎯 Data Flow v1.4.0
```
Frontend Form Data → API Request Format → .NET API → PostgreSQL Database
     ↓                     ↓                  ↓              ↓
- formSaleOrder        - TbtSaleOrder     - Service      - Database
- stockItems           - JSON in data     - Controller   - Tables
- copyItems            - Field mapping    - Store        - Records
```

### 📊 API Integration Details v1.4.0
- **Store File**: `src/stores/modules/api/sale/sale-order-store.js`
- **API Endpoints**: 
  - `POST /SaleOrder/Upsert` - สร้าง/อัปเดตใบสั่งขาย
  - `POST /SaleOrder/Get` - ดึงข้อมูลใบสั่งขาย
  - `POST /SaleOrder/List` - รายการใบสั่งขาย
- **Error Handling**: SweetAlert2 สำหรับแสดงผลลัพธ์
- **Loading States**: แสดง loading indicator ระหว่างการเรียก API

### 💾 Data Mapping v1.4.0
```javascript
// Frontend → API
formSaleOrder.number → soNumber
formSaleOrder.expectedDeliveryDate → deliveryDate
formSaleOrder.customerName → customerName
formSaleOrder.quotationNumber → refQuotation
stockItems + copyItems → data (JSON string)

// API → Frontend
soNumber → formSaleOrder.number
deliveryDate → formSaleOrder.expectedDeliveryDate
customerName → formSaleOrder.customerName
data (JSON) → stockItems + copyItems
```

## การอัปเดตก่อนหน้า (v1.3.2)

### ✨ New Features v1.3.2
- **Converted Currency Integration**: ใบสรุปใบสั่งขายใช้ราคาที่แปลงสกุลเงินแล้ว
- **Freight Inclusion**: ยอดรวมใบสั่งขายรวม freight และ insurance แล้ว
- **Enhanced Price Calculation**: ปรับปรุงการคำนวณราคาให้แม่นยำมากขึ้น

### 🔧 Technical Changes v1.3.2
- **selectedItemsTotal()**: ใช้ `getTotalConvertedPrice()` แทนการคูณราคาธรรมดา
- **totalOrderAmount()**: รวม freight จากฟิลด์ `formSaleOrder.freight`
- **calculateGrandTotal()**: มีการใช้ราคาแปลงแล้วอยู่แล้ว (ไม่เปลี่ยน)

### 📊 Price Calculation Updates v1.3.2
| ฟังก์ชัน | การเปลี่ยนแปลง | ผลลัพธ์ |
|----------|----------------|---------|
| `selectedItemsTotal()` | ใช้ `getTotalConvertedPrice()` | ราคาแปลงแล้วสำหรับ stock items |
| `totalOrderAmount()` | + freight addition | ยอดรวมใบสั่งขาย + ค่าขนส่ง |
| Order Summary Display | รวม freight & insurance | แสดงราคาสุทธิที่ลูกค้าจ่าย |

## การอัปเดตก่อนหน้า (v1.3.1)

### 🔧 Bug Fixes v1.3.1
- **Footer Sum Functions Fixed**: แก้ไขฟังก์ชัน sum ใน footer ให้แสดงทศนิยม 2 ตำแหน่งอย่างถูกต้อง
- **Runtime Error Fixed**: แก้ไข `.toFixed() is not a function` error ด้วย null/undefined checks
- **Order Summary Calculation**: ปรับใบสรุปใบสั่งขายให้รวมเฉพาะ stock items เท่านั้น (ไม่รวม copy items)
- **Decimal Precision**: เพิ่ม `.toFixed(2)` ในฟังก์ชัน calculation ทั้งหมด พร้อม error handling

### 📊 Summary Functions Updated
| ฟังก์ชัน | เปลี่ยนแปลง | ผลลัพธ์ |
|----------|-------------|---------|
| `getSumAppraisalPrice()` | + null checks + `.toFixed(2)` | '0.00' หรือทศนิยม 2 ตำแหน่ง |
| `getSumDiscountPrice()` | + null checks + `.toFixed(2)` | '0.00' หรือทศนิยม 2 ตำแหน่ง |
| `getSumConvertedPrice()` | + null checks + `.toFixed(2)` | '0.00' หรือทศนิยม 2 ตำแหน่ง |
| `getSumTotalConvertedPrice()` | + null checks + `.toFixed(2)` | '0.00' หรือทศนิยม 2 ตำแหน่ง |
| `getSumQty()` | + null checks | 0 หรือจำนวนเต็ม |

### 🛡️ Error Handling Improvements
- **Null/Undefined Checks**: ตรวจสอบ `items` array ก่อนทำ calculation
- **Array Validation**: ตรวจสอบว่าเป็น Array และมีความยาว > 0
- **Number Conversion**: ใช้ `Number()` wrapper เพื่อป้องกัน NaN
- **Default Values**: Return ค่าเริ่มต้นเมื่อไม่มีข้อมูล ('0.00' สำหรับราคา, 0 สำหรับจำนวน)

### 🎯 Order Summary Changes
- **selectedItemsTotal()**: รวมเฉพาะ stock items
- **selectedItemsCount()**: นับเฉพาะ stock items  
- **calculateGrandTotal()**: รวมเฉพาะ stock items + freight
- **totalOrderAmount()**: ยอดรวมใบสั่งขายจาก stock items เท่านั้น

### 💡 การใช้งาน v1.3.1
- **Footer Stock Table**: แสดงยอดรวมทศนิยม 2 ตำแหน่งแล้ว
- **Footer Copy Items**: แสดงยอดรวมทศนิยม 2 ตำแหน่งแล้ว
- **ใบสรุปใบสั่งขาย**: รวมเฉพาะสินค้าคงคลัง ไม่รวมสินค้าสำเนา
- **Copy Items**: มีการคำนวณแยกต่างหาก ไม่ส่งผลต่อยอดใบสั่งขาย

## การอัปเดตก่อนหน้า (v1.3.0)

### ✨ เพิ่มฟีเจอร์ใหม่ v1.3.0
- **Copy Items Table ปรับปรุงใหม่**: ตาราง "สินค้าสำเนา" เหมือน stock table ทุกอย่าง
- **ColumnGroup Header**: เพิ่ม header structure แบบเดียวกับ stock table
- **Editable Copy Items**: แก้ไขได้ทุกฟิลด์ - รหัสสินค้า, รายละเอียด, ราคา, จำนวน
- **Copy Items Footer**: สรุปน้ำหนักและราคาแยกสำหรับสินค้าสำเนา
- **Copy Freight**: ค่าขนส่งแยกสำหรับสินค้าสำเนา
- **Separate Calculations**: การคำนวณแยกระหว่างสินค้าคงคลังและสินค้าสำเนา

### 🔧 Technical Improvements v1.3.0
- **Identical Table Structure**: copy items table เหมือน stock table 100%
- **17 Columns Layout**: ใช้โครงสร้างคอลัมน์เดียวกันทุกประการ
- **Action Buttons**: ลบ, แก้ไข, สร้างใบสั่งผลิต
- **Copy-specific Methods**: เพิ่ม `onBlurCopyPrice()`, `onBlurCopyQty()`, `calculateCopyTotal()`
- **Enhanced Footer**: รวม 3 แถว - summary, freight, grand total
- **Production Indicator**: แสดง "ต้องผลิต" ในคอลัมน์เลขที่ผลิต

### 📋 Copy Items Table Features
1. **Structure**: ColumnGroup + 17 คอลัมน์ + footer calculations
2. **Action Buttons**: 
   - 🗑️ ลบ (btn-red)
   - 🖌️ แก้ไข (btn-main)
   - 🔧 สร้างใบสั่งผลิต (btn-warning)
3. **Editable Fields**: รหัสสินค้า, รายละเอียด, ราคาประเมิน, จำนวน
4. **Footer Calculations**: 
   - Net Weight + ทอง + เพชร + พลอย
   - Freight & Insurance (Copy Items)
   - ราคารวม (สินค้าสำเนา)
5. **Material Display**: แสดงวัตถุดิบเหมือน stock table

### 🎯 Copy Items vs Stock Items
| ฟีเจอร์ | Stock Items | Copy Items |
|---------|-------------|------------|
| Table Structure | ✅ 17 คอลัมน์ | ✅ 17 คอลัมน์ |
| ColumnGroup Header | ✅ | ✅ |
| Footer Calculations | ✅ | ✅ |
| Editable Fields | ✅ | ✅ |
| Action Buttons | ลบ,แก้ไข,คัดลอก | ลบ,แก้ไข,ผลิต |
| เลขที่ผลิต | แสดงจริง | "ต้องผลิต" |
| Freight | แยกต่างหาก | แยกต่างหาก |
| Badge Color | Success (เขียว) | Warning (เหลือง) |

### 👥 Customer Management Features (v1.2.0)
1. **Customer Search Modal**: ค้นหาลูกค้าจากฐานข้อมูล
2. **Customer Create Modal**: เพิ่มลูกค้าใหม่
3. **Auto-populate**: เติมข้อมูลลูกค้าอัตโนมัติเมื่อเลือก
4. **Display Fields**: แสดงข้อมูลลูกค้าแบบ read-only
5. **Customer Remark**: ช่องหมายเหตุลูกค้า

### 🎯 การใช้งาน v1.3.0
1. **สินค้าคงคลัง**: ใช้ตาราง "รายการสินค้า" (badge เขียว)
2. **สินค้าสำเนา**: ใช้ตาราง "สินค้าสำเนา" (badge เหลือง)
3. **แก้ไขสินค้าสำเนา**: คลิกปุ่ม "แก้ไข" (brush icon)
4. **สร้างใบสั่งผลิต**: คลิกปุ่ม "สร้างใบสั่งผลิต" (tools icon)
5. **ราคาแยก**: freight และยอดรวมแยกสำหรับแต่ละประเภท

## การอัปเดตก่อนหน้า (v1.2.0)

### ✨ เพิ่มฟีเจอร์ใหม่ v1.2.0
- **ตาราง Stock เหมือน Quotation**: ปรับปรุงตารางสินค้าให้เหมือนกับระบบ quotation
- **Customer Management**: เพิ่มระบบจัดการลูกค้าเหมือน quotation (ค้นหา + เพิ่มใหม่)
- **Edit Stock Modal**: เพิ่ม modal แก้ไขข้อมูลสินค้าแบบเดียวกับ quotation
- **Copy Item Function**: เพิ่มฟังก์ชันคัดลอกสินค้า
- **Image Preview**: เพิ่มการแสดงรูปภาพสินค้าในตาราง
- **Price Calculation**: ปรับปรุงระบบคำนวณราคาให้เหมือน quotation

### 🔧 Technical Improvements v1.2.0
- **Quotation-like Table Structure**: ใช้ ColumnGroup และ footer totals
- **Material Display**: แสดงข้อมูลวัตถุดิบ (ทอง, เพชร, พลอย) แบบเดียวกับ quotation
- **Editable Fields**: ช่องแก้ไขราคาประเมิน, รายละเอียด, จำนวน
- **Currency Conversion**: ระบบแปลงสกุลเงินและคำนวณราคา
- **Footer Calculations**: สรุปน้ำหนัก, ราคา, freight และยอดรวม

---

## การอัปเดตก่อนหน้า (v1.1.0)

### ✨ เพิ่มฟีเจอร์ใหม่
- **Modal เลือกใบเสนอราคา**: ใช้ pattern เดียวกันกับ update-plan-view.vue
- **การค้นหาขั้นสูง**: Dialog inside Modal พร้อมตัวกรองเพิ่มเติม
- **BaseDataTable Integration**: ใช้ component เดียวกันกับระบบอื่น
- **API Store Integration**: เชื่อมต่อ usrQuotationApiStore สำหรับดึงข้อมูล

### 🔧 Technical Improvements
- **Standard Modal Pattern**: ใช้ `@/components/modal/ModalView.vue`
- **Consistent Styling**: รักษา SCSS style ให้สอดคล้องกับระบบ
- **Clean Component Structure**: แยก modal เป็น component ชัดเจน
- **Proper Event Handling**: ใช้ emit patterns ตาม Vue 3 best practices

### 📋 Modal Features Summary
1. **ฟอร์มค้นหาหลัก**: เลขที่ใบเสนอราคา
2. **ค้นหาขั้นสูง**: ชื่อลูกค้า, สกุลเงิน, ผู้สร้าง, วันที่
3. **ตารางแสดงผล**: 12 คอลัมน์พร้อม pagination และ sorting
4. **การเลือกข้อมูล**: ปุ่มเลือกสีเขียวในแต่ละรายการ
5. **Responsive Design**: ปรับขนาดตามหน้าจอ

### 🎯 การใช้งาน
1. คลิกปุ่ม "ใบเสนอราคา" (สีเขียว) → Modal เปิดขึ้น
2. ค้นหาด้วยเลขที่ หรือ คลิก "เพิ่มเติม" สำหรับตัวกรองขั้นสูง
3. เลือกใบเสนอราคาจากตาราง → ข้อมูลโหลดมาสร้าง Sale Order
4. Modal ปิดอัตโนมัติหลังเลือกข้อมูล