# Quotation List System Documentation

ระบบแสดงรายการใบเสนอราคาสำหรับธุรกิจเครื่องประดับ

## โครงสร้างไฟล์

```
quotation-list/
├── index-view.vue                    # หน้าหลัก - จัดการ state และ event
├── components/
│   ├── search-view.vue              # ฟอร์มค้นหาและกรองข้อมูลแบบ dialog
│   └── data-table-view.vue          # ตารางแสดงรายการใบเสนอราคา
└── quotationlist.md                 # เอกสารการใช้งาน
```

## ฟีเจอร์หลัก

### 1. การค้นหาและกรองข้อมูล (Search & Filter)

#### การค้นหาหลัก
- **เลขที่ใบเสนอราคา**: ค้นหาด้วยหมายเลขใบเสนอราคา (แสดงในหน้าหลัก)

#### การค้นหาเพิ่มเติม (Dialog)
- **ชื่อลูกค้า**: ค้นหาด้วยชื่อลูกค้า
- **สกุลเงิน**: กรองตามสกุลเงิน (USD, THB, etc.)
- **ผู้สร้าง**: ค้นหาตามชื่อผู้สร้าง
- **วันที่สร้าง**: ช่วงวันที่สร้างใบเสนอราคา
- **วันที่ใบเสนอราคา**: ช่วงวันที่ของใบเสนอราคา

#### ปุ่มการทำงาน
- **🔍 ค้นหา**: ทำการค้นหาข้อมูลตามเงื่อนไข
- **🔍+ เพิ่มเติม**: เปิด dialog สำหรับตัวกรองเพิ่มเติม
- **❌ ล้าง**: ล้างเงื่อนไขการค้นหาทั้งหมด

### 2. ตารางรายการใบเสนอราคา (Quotation List Table)

#### คอลัมน์หลัก
| คอลัมน์ | รายละเอียด | การจัดเรียง |
|---------|------------|-------------|
| เลขที่ใบเสนอราคา | หมายเลขใบเสนอราคา | ✅ |
| เลขที่รัน | เลขที่รันของระบบ | ✅ |
| ชื่อลูกค้า | ชื่อลูกค้า | ✅ |
| เบอร์โทร | หมายเลขโทรศัพท์ลูกค้า | ✅ |
| อีเมล | อีเมลลูกค้า | ✅ |
| สกุลเงิน | สกุลเงินที่ใช้ | ✅ |
| อัตราแลกเปลี่ยน | อัตราแลกเปลี่ยนเงิน | ✅ |
| Markup (%) | อัตรากำไร | ✅ |
| ส่วนลด (%) | เปอร์เซ็นต์ส่วนลด | ✅ |
| ค่าขนส่ง | ค่าขนส่งและประกัน | ✅ |
| วันที่ใบเสนอราคา | วันที่ของใบเสนอราคา | ✅ |
| วันที่สร้าง | วันที่สร้างในระบบ | ✅ |
| ผู้สร้าง | ชื่อผู้สร้าง | ✅ |
| หมายเหตุ | ข้อมูลเพิ่มเติม | ✅ |

#### การดำเนินการ (Actions)
- **แก้ไข**: นำทางไปยังหน้าแก้ไขใบเสนอราคา
- **ดูรายละเอียด**: นำทางไปยังหน้าดูรายละเอียดใบเสนอราคา

### 3. ฟีเจอร์ตาราง

#### การจัดเรียงข้อมูล (Sorting)
- รองรับการจัดเรียงทุกคอลัมน์
- สามารถจัดเรียงหลายคอลัมน์พร้อมกัน
- แสดงลูกศรบอกทิศทางการจัดเรียง

#### การแบ่งหน้า (Pagination)
- แสดง 10 รายการต่อหน้าเป็นค่าเริ่มต้น
- รองรับการเปลี่ยนจำนวนรายการต่อหน้า
- แสดงจำนวนรายการทั้งหมด
- การนำทางระหว่างหน้า

## API Integration

### Backend API (C# .NET)

#### Endpoint
```
POST /Quotation/List
```

#### Request Model (`jewelry.Model.Sale.Quotation.List.Request`)
```csharp
public class Request : DataSourceRequest
{
   public Search Search { get; set; } 
}

public class Search
{
    public string? Number { get; set; }                    // เลขที่ใบเสนอราคา
    public string? CustomerName { get; set; }              // ชื่อลูกค้า
    public DateTime? CreateDateStart { get; set; }         // วันที่สร้าง (เริ่มต้น)
    public DateTime? CreateDateEnd { get; set; }           // วันที่สร้าง (สิ้นสุด)
    public DateTime? QuotationDateStart { get; set; }      // วันที่ใบเสนอราคา (เริ่มต้น)
    public DateTime? QuotationDateEnd { get; set; }        // วันที่ใบเสนอราคา (สิ้นสุด)
    public string? Currency { get; set; }                  // สกุลเงิน
    public string? CreateBy { get; set; }                  // ผู้สร้าง
}
```

#### Response Model (`jewelry.Model.Sale.Quotation.List.Response`)
```csharp
public class Response
{
    public string Number { get; set; }                     // เลขที่ใบเสนอราคา
    public string Running { get; set; }                    // เลขที่รัน
    public string CustomerName { get; set; }               // ชื่อลูกค้า
    public string CustomerPhone { get; set; }              // เบอร์โทร
    public string CustomerEmail { get; set; }              // อีเมล
    public string CustomerAddress { get; set; }            // ที่อยู่
    public string Currency { get; set; }                   // สกุลเงิน
    public decimal CurrencyRate { get; set; }              // อัตราแลกเปลี่ยน
    public decimal MarkUp { get; set; }                     // อัตรากำไร
    public decimal Discount { get; set; }                  // ส่วนลด
    public decimal? Freight { get; set; }                  // ค่าขนส่ง
    public string Remark { get; set; }                     // หมายเหตุ
    public DateTime? Date { get; set; }                    // วันที่ใบเสนอราคา
    public DateTime CreateDate { get; set; }               // วันที่สร้าง
    public string CreateBy { get; set; }                   // ผู้สร้าง
    public DateTime? UpdateDate { get; set; }              // วันที่แก้ไข
    public string UpdateBy { get; set; }                   // ผู้แก้ไข
}
```

### Frontend Store (Pinia)

#### Store: `usrQuotationApiStore`

**Actions:**
- `fetchList({ take, skip, sort, formValue })`: ดึงข้อมูลรายการใบเสนอราคา

**State:**
```javascript
{
  dataList: {
    data: [],        // รายการข้อมูล
    total: 0         // จำนวนทั้งหมด
  }
}
```

#### รูปแบบ Request ใหม่
```javascript
{
  take: 10,
  skip: 0,
  sort: [{ field: 'createDate', dir: 'desc' }],
  search: {
    number: "QUO-2025-001",
    customerName: "ลูกค้า", 
    currency: "THB",
    createBy: "admin",
    createDateStart: "2024-01-01T00:00:00.000Z",
    createDateEnd: "2024-12-31T23:59:59.999Z",
    quotationDateStart: "2024-01-01T00:00:00.000Z",
    quotationDateEnd: "2024-12-31T23:59:59.999Z"
  }
}
```

## การใช้งาน

### 1. เข้าสู่หน้ารายการใบเสนอราคา
1. เข้าสู่เมนู "งานขาย" > "รายการใบเสนอราคา"
2. ระบบจะแสดงรายการใบเสนอราคาทั้งหมดที่เรียงตามวันที่สร้างล่าสุด

### 2. การค้นหาและกรองข้อมูล
1. **ค้นหาหลัก**: กรอกเลขที่ใบเสนอราคาในช่องค้นหาหลัก
2. **ค้นหาเพิ่มเติม**: คลิกปุ่ม "เพิ่มเติม" (🔍+) เพื่อเปิด dialog
3. กรอกเงื่อนไขการค้นหาในฟิลด์ต่างๆ ภายใน dialog
4. คลิกปุ่ม "ค้นหา" (🔍) เพื่อดำเนินการค้นหา
5. คลิกปุ่ม "ล้าง" (❌) เพื่อล้างเงื่อนไขค้นหา

### 3. การจัดการรายการ
1. **แก้ไข**: คลิกปุ่ม "แก้ไข" (🖌️) เพื่อแก้ไขใบเสนอราคา
2. **ดูรายละเอียด**: คลิกปุ่ม "ดูรายละเอียด" (👁️) เพื่อดูข้อมูลแบบอ่านอย่างเดียว

### 4. การจัดเรียงข้อมูล
1. คลิกที่หัวคอลัมน์ที่ต้องการจัดเรียง
2. คลิกอีกครั้งเพื่อเปลี่ยนทิศทางการจัดเรียง (จากน้อยไปมาก/มากไปน้อย)
3. สามารถจัดเรียงหลายคอลัมน์โดยกดปุ่ม Ctrl ค้างไว้ขณะคลิก

## UI/UX Improvements

### Dialog-based Advanced Search
- **เลขที่ใบเสนอราคา**: แสดงในหน้าหลัก สำหรับการค้นหาเบื้องต้น
- **ตัวกรองอื่นๆ**: ย้ายไปใน dialog เพื่อลดความซับซ้อนของ UI
- **ปุ่มเพิ่มเติม**: ใช้ไอคอน 🔍+ สำหรับเปิด dialog ค้นหาขั้นสูง

### Consistent Component Usage
- ใช้ `DialogSearchView` component เดียวกันกับระบบอื่นๆ
- รักษารูปแบบการใช้งานให้สอดคล้องกับ stock gem search
- ใช้ styling กลางจาก `@/assets/scss/custom-style/`

## ข้อมูลเทคนิค

### Dependencies
- **Vue 3**: Composition API และ Reactivity
- **PrimeVue**: DataTable, Calendar, Dialog components
- **Pinia**: การจัดการ state
- **Vue Router**: การนำทางระหว่างหน้า

### การจัดเก็บข้อมูล
- ใช้ฐานข้อมูล PostgreSQL
- ตาราง: `TbtSaleQuotation`
- การเชื่อมต่อผ่าน Entity Framework Core

### Performance Features
- **Lazy Loading**: โหลดข้อมูลแบบแบ่งหน้า
- **Server-side Filtering**: กรองข้อมูลที่เซิร์ฟเวอร์
- **Dynamic Sorting**: จัดเรียงข้อมูลแบบไดนามิก
- **Dialog-based UI**: ลดความซับซ้อนของ main interface

## เส้นทางการนำทาง (Routing)

### URL
```
/sale-quotation-list
```

### Route Configuration
```javascript
{
  path: '/sale-quotation-list',
  name: 'sale-quotation-list',
  component: QuotationList,
  meta: {
    Displayname: {
      en: 'Quotation List',
      th: 'รายการใบเสนอราคา'
    },
    minorShow: true,
    permissions: [PERMISSIONS.SALE_VIEW]
  }
}
```

### สิทธิ์การเข้าถึง
- ต้องมีสิทธิ์ `SALE_VIEW`
- ระบบจะตรวจสอบสิทธิ์ก่อนเข้าถึงหน้า

## การบำรุงรักษา

### การเพิ่มคอลัมน์ใหม่
1. เพิ่มฟิลด์ใน Response model (Backend)
2. เพิ่มคอลัมน์ใน `columns` array (Frontend)
3. เพิ่ม template หากต้องการจัดรูปแบบพิเศษ

### การเพิ่มตัวกรองใหม่
1. เพิ่มฟิลด์ใน Search model (Backend)
2. เพิ่ม input field ใน dialog content (search-view.vue)
3. เพิ่ม logic ใน QuotationService.List method
4. อัปเดต store method สำหรับส่งข้อมูลใหม่

### การปรับแต่งการแสดงผล
1. แก้ไขไฟล์ `data-table-view.vue`
2. เพิ่ม custom template สำหรับคอลัมน์
3. ปรับแต่ง CSS ใน style section
4. อัปเดต dialog layout ใน `search-view.vue`

### การเพิ่ม Filter Field ใหม่
1. เพิ่มใน dialog content template
2. อัปเดต form data model
3. อัปเดต store `fetchList` method
4. เพิ่ม backend Search model property

---
*อัปเดตเมื่อ: 2025-01-15*
*เวอร์ชัน: 2.0.0*
*UI Pattern: Dialog-based Advanced Search*