# Camera Troubleshooting Guide - Mobile Scan Feature

## ปัญหา: กล้องเปิดไม่ได้

### 1. ตรวจสอบ URL ที่เข้าใช้งาน

**ปัญหา**: Browser ต้องการ HTTPS หรือ localhost เท่านั้นสำหรับ camera access

**วิธีแก้:**

#### Development (Local)
```
✅ ถูกต้อง:
- http://localhost:5173
- http://localhost:3000
- http://127.0.0.1:5173

❌ ไม่ได้:
- http://192.168.1.100:5173  (IP address ไม่ใช่ localhost)
- http://yourname.local:5173
```

#### Production
```
✅ ต้องใช้ HTTPS:
- https://yourdomain.com
- https://subdomain.yourdomain.com

❌ HTTP ไม่ได้:
- http://yourdomain.com
```

**วิธีทดสอบบน Mobile ใน Development:**

1. **ใช้ ngrok** (แนะนำ):
```bash
# Install ngrok
npm install -g ngrok

# Run ngrok
ngrok http 5173

# จะได้ HTTPS URL เช่น: https://abc123.ngrok.io
```

2. **ใช้ Cloudflare Tunnel**:
```bash
# Install
npm install -g cloudflared

# Run tunnel
cloudflared tunnel --url http://localhost:5173
```

### 2. ตรวจสอบ Camera Permission

#### Chrome (Desktop)
1. คลิกไอคอน 🔒 หรือ 🎥 ที่แถบ URL
2. ดูว่า Camera = "อนุญาต" หรือไม่
3. ถ้าเป็น "บล็อก" ให้เปลี่ยนเป็น "อนุญาต"
4. Refresh หน้าเว็บ

#### Chrome (Mobile - Android)
1. ไปที่ Settings > Site Settings > Camera
2. หา URL ของคุณ
3. เลือก "อนุญาต"
4. Refresh หน้าเว็บ

#### Safari (iOS)
1. ไปที่ Settings > Safari > Camera
2. เลือก "Allow" หรือ "Ask"
3. Refresh หน้าเว็บ
4. กด "Allow" เมื่อมี popup ขอสิทธิ์

### 3. ตรวจสอบกล้องของอุปกรณ์

**ทดสอบว่ากล้องทำงานหรือไม่:**

1. เปิดแอพกล้องของระบบ (Camera app)
2. ถ้ากล้องทำงาน → ปัญหาอยู่ที่ browser permission
3. ถ้ากล้องไม่ทำงาน → ปัญหาอยู่ที่ hardware/OS

### 4. ตรวจสอบว่ามีแอพอื่นใช้กล้องอยู่หรือไม่

**Mobile:**
- ปิดแอพทุกตัวที่อาจใช้กล้อง (เช่น Camera, Instagram, Zoom, etc.)
- Restart เบราว์เซอร์

**Desktop:**
- ปิดโปรแกรมที่ใช้ webcam (Zoom, Skype, OBS, etc.)
- Restart เบราว์เซอร์

### 5. Browser Compatibility

**รองรับ:**
- ✅ Chrome/Edge (Desktop & Mobile) - แนะนำ
- ✅ Safari (iOS 11+)
- ✅ Firefox (Desktop & Mobile)

**ไม่รองรับ:**
- ❌ In-app browsers (Facebook, LINE, Instagram browser)
- ❌ Old browsers (IE, old Safari)

**วิธีแก้สำหรับ In-app browser:**
1. คัดลอก URL
2. เปิดใน Safari หรือ Chrome โดยตรง

### 6. Debug Information

เมื่อกด "เปิดกล้อง" ระบบจะแสดงข้อมูล debug เช่น:
```
Protocol: http:, Host: localhost, Secure: true
```

**ตรวจสอบว่า:**
- `Secure: true` (สำคัญมาก!)
- `Protocol: https:` หรือ `Host: localhost`

### 7. Console Error Messages

เปิด Developer Console (F12) และดู error messages:

**Common Errors:**

#### "NotAllowedError" / "PermissionDeniedError"
```
✅ แก้ไข: อนุญาต camera permission ตามขั้นตอนข้อ 2
```

#### "NotFoundError" / "DevicesNotFoundError"
```
✅ แก้ไข: ตรวจสอบกล้องของอุปกรณ์ (ข้อ 3)
```

#### "NotReadableError" / "TrackStartError"
```
✅ แก้ไข: กล้องถูกใช้โดยแอพอื่น ให้ปิดแอพนั้นก่อน
```

#### "NotSupportedError"
```
✅ แก้ไข: ใช้ HTTPS หรือ localhost (ข้อ 1)
```

### 8. Mobile-Specific Issues

#### iOS Safari
- **ปัญหา**: Safari บางเวอร์ชันต้องการ user gesture (click) ก่อนเปิดกล้อง
- **วิธีแก้**: กดปุ่ม "เปิดกล้อง" อีกครั้ง

#### Android Chrome
- **ปัญหา**: Permission popup อาจถูกบล็อก
- **วิธีแก้**: ไปตั้งค่า permission แบบ manual (ขั้นตอนข้อ 2)

### 9. Testing Steps

#### Local Development
```bash
# 1. Start dev server
npm run dev

# 2. เปิดใน browser
http://localhost:5173/mobile/scan

# 3. เลือก "สแกนสินค้า"

# 4. กด "เปิดกล้อง"

# 5. อนุญาต permission (ครั้งแรก)

# 6. ดูว่ากล้องเปิดหรือไม่
```

#### Mobile Testing (with ngrok)
```bash
# 1. Start dev server
npm run dev

# 2. Start ngrok
ngrok http 5173

# 3. เปิด HTTPS URL ที่ได้ใน mobile browser
https://abc123.ngrok.io/mobile/scan

# 4. ทำตามขั้นตอน 3-6 ด้านบน
```

### 10. Fallback Solution

ถ้ากล้องยังเปิดไม่ได้ ให้ใช้ **Manual Input** แทน:

1. Scroll ลงมาใต้ส่วนกล้อง
2. จะเห็นข้อความ "หรือ"
3. กรอก Stock Number หรือ Product Code
4. กด "ค้นหา" หรือ Enter

## Summary Checklist

เมื่อกล้องเปิดไม่ได้ ให้ตรวจสอบตามลำดับ:

- [ ] ใช้ HTTPS หรือ localhost (ข้อ 1)
- [ ] Browser permission = "อนุญาต" (ข้อ 2)
- [ ] กล้องของอุปกรณ์ทำงานปกติ (ข้อ 3)
- [ ] ไม่มีแอพอื่นใช้กล้องอยู่ (ข้อ 4)
- [ ] ใช้ browser ที่รองรับ (ข้อ 5)
- [ ] ดู debug info และ console errors (ข้อ 6-7)
- [ ] ลอง refresh หน้าเว็บ
- [ ] ลอง restart browser
- [ ] ลองใช้ Manual Input แทน (ข้อ 10)

## Contact

หากยังแก้ไม่ได้ ให้บันทึกข้อมูลต่อไปนี้:

1. Browser name & version (เช่น Chrome 120)
2. Device (เช่น iPhone 14, Samsung Galaxy S23)
3. OS version (เช่น iOS 17, Android 13)
4. URL ที่เข้าใช้งาน
5. Debug info จากหน้า scan
6. Screenshot ของ error message
7. Console error messages (F12)
